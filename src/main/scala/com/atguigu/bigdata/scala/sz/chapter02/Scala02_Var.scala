package com.atguigu.bigdata.scala.sz.chapter02

object Scala02_Var {
  def main(args: Array[String]): Unit = {
    //TODO 变量
    //Scala语言认为类型没有那么重要，可以通过编译器自动推断出来所有类型可以省略的
    //强制类型转换条件：子父类关系，或其他关系，Integer str = Integer.parseInt(s);

    val i = "abc"
    println(i)

  }
}
